services:
  - type: web
    name: sports-predictor
    env: node
    buildCommand: cd server && npm install
    startCommand: node server/server.js
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
